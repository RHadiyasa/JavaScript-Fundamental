// Menggunakan looping untuk mengakses index dalam sebuah array

const heroes = ["Miya", "Layla", "Moskov", "Kimmy", "Clint"];

for (let i = 0; i < heroes.length; i++) {
  const currentHero = heroes[i];
  console.log(`${currentHero} adalah marksman`);
}
