// Concatenating -> Gabungin string

const firstName = "Rafi";
const lastName = "Hadiyasa";
console.log(firstName + " " + lastName);

// Kita bisa menggunakan backtick ``
// Dengan back tick kita bisa menggunaka template literal
console.log(`${firstName} ${lastName}`);

const x = 5;
const y = "3";
const z = 2;
console.log(x + z + y); 
console.log(x + y + z); // format number tetap dijumlahkan seperti biasa
